import MainTemplate from "../templates/MainTemplate";

const SiteMap = () => {
    return (
        <MainTemplate>
            <h1>Site Map</h1>
        </MainTemplate>
    );
}

export default SiteMap;
